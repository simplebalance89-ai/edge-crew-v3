apiVersion: v1
kind: Service
metadata:
  name: data-ingestion
  namespace: edge-crew
  labels:
    app: data-ingestion
    component: service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: data-ingestion
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-ingestion
  namespace: edge-crew
  labels:
    app: data-ingestion
    component: service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: data-ingestion
  template:
    metadata:
      labels:
        app: data-ingestion
        component: service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - data-ingestion
              topologyKey: kubernetes.io/hostname
      containers:
      - name: data-ingestion
        image: edgecrew/data-ingestion:latest
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: SERVER_PORT
          value: "8080"
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: edge-crew-config
              key: log.level
        - name: REDIS_URL
          valueFrom:
            configMapKeyRef:
              name: edge-crew-config
              key: redis.url
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: edge-crew-secrets
              key: postgres.url
        - name: API_KEY_INTERNAL
          valueFrom:
            secretKeyRef:
              name: edge-crew-secrets
              key: api.key.internal
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 12
